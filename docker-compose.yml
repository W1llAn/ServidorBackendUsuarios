version: "3.8"

services:
  # Base de datos PostgreSQL
  postgres:
    image: postgres:15-alpine
    container_name: microservicios_postgres
    environment:
      POSTGRES_DB: microservicios_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - microservicios_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  # Servicio de Usuarios
  usuarios-service:
    build: ./services/usuarios-service
    container_name: usuarios_service
    environment:
      PORT: 3001
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: microservicios_db
      DB_USER: postgres
      DB_PASSWORD: postgres
    ports:
      - "3001:3001"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - microservicios_network

  # Servicio de Departamentos
  departamentos-service:
    build: ./services/departamentos-service
    container_name: departamentos_service
    environment:
      PORT: 3002
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: microservicios_db
      DB_USER: postgres
      DB_PASSWORD: postgres
      JWT_SECRET: tu_clave_secreta_super_segura_cambiala_en_produccion
    ports:
      - "3002:3002"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - microservicios_network

  # Servicio de Autenticación
  auth-service:
    build: ./services/auth-service
    container_name: auth_service
    environment:
      PORT: 3003
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: microservicios_db
      DB_USER: postgres
      DB_PASSWORD: postgres
      JWT_SECRET: tu_clave_secreta_super_segura_cambiala_en_produccion
      JWT_EXPIRES_IN: 24h
      JWT_REFRESH_EXPIRES_IN: 7d
      SESSION_SECRET: tu_session_secret_super_segura_cambiala_en_produccion
      # Variables OAuth 2.0 de Google (debes configurarlas)
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-tu_google_client_id}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:-tu_google_client_secret}
      GOOGLE_CALLBACK_URL: ${GOOGLE_CALLBACK_URL:-http://localhost:3000/api/auth/google/callback}
      FRONTEND_URL: ${FRONTEND_URL:-http://127.0.0.1:5500}
    ports:
      - "3003:3003"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - microservicios_network

  # API Gateway
  api-gateway:
    build: ./api-gateway
    container_name: api_gateway
    environment:
      PORT: 3000
      AUTH_SERVICE_URL: http://auth-service:3003
      USERS_SERVICE_URL: http://usuarios-service:3001
      DEPARTMENTS_SERVICE_URL: http://departamentos-service:3002
    ports:
      - "3000:3000"
    depends_on:
      - auth-service
      - usuarios-service
      - departamentos-service
    networks:
      - microservicios_network

networks:
  microservicios_network:
    driver: bridge

volumes:
  postgres_data:
